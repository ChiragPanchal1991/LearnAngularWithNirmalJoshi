import { TextDecoraterDirective } from './text-decorater.directive';

describe('TextDecoraterDirective', () => {
  it('should create an instance', () => {
    const directive = new TextDecoraterDirective();
    expect(directive).toBeTruthy();
  });
});
